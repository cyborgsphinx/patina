//extern crate libc;

//use self::libc::funcs::posix01::signal;
//use self::libc::types::os::common::posix01;

#[allow(dead_code)]
pub fn catch_signal(_signo: isize) {
    println!(""); // simplistic, but works
}

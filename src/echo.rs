use std::string::String;

pub fn parse(args: &[&str]) -> String {
    // parse strings for flags, variable expansion, etc.
    let st = String::new();
    return st;
}

pub fn put(out: String) {
    // output string as parsed
}
